import XCTest

final class SessionServiceTests: XCTestCase {
    func testSanity() {
        // Minimal sanity test to verify test target/scheme configuration.
        XCTAssertTrue(1 + 1 == 2)
    }
}
